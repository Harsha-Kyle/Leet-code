class Solution(object):
    def clearDigits(self, s):
        l=""
        for i in s:
            if i in ['1','2','3','4','5','6','7','8','9','0']:
                l=l[0:len(l)-1]
            else:
                l+=i
        return l
        
